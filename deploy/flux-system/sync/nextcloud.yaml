apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: nextcloud
spec:
  sourceRef:
    kind: GitRepository
    name: github-homes
  path: ./deploy/nextcloud
  prune: true
  interval: 1h
  decryption:
    provider: sops
    secretRef:
      name: sops-age
